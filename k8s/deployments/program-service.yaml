apiVersion: apps/v1
kind: Deployment
metadata:
  name: program-service
  namespace: powerlifting-coach
  labels:
    app: program-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: program-service
  template:
    metadata:
      labels:
        app: program-service
    spec:
      containers:
      - name: program-service
        image: ghcr.io/powerlifting-coach-app/program-service:latest
        ports:
        - containerPort: 8084
        env:
        - name: PORT
          value: "8084"
        - name: ENVIRONMENT
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: database-url
        - name: REDIS_URL
          value: "valkey:6379"
        - name: LITELLM_URL
          value: "http://open-webui:8080"
        - name: AUTH_SERVICE
          value: "http://auth-service:8080"
        - name: USER_SERVICE
          value: "http://user-service:8081"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: program-service
  namespace: powerlifting-coach
spec:
  selector:
    app: program-service
  ports:
  - port: 8084
    targetPort: 8084
  type: ClusterIP