apiVersion: apps/v1
kind: Deployment
metadata:
  name: settings-service
  namespace: powerlifting-coach
  labels:
    app: settings-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: settings-service
  template:
    metadata:
      labels:
        app: settings-service
    spec:
      containers:
      - name: settings-service
        image: ghcr.io/powerlifting-coach-app/settings-service:latest
        ports:
        - containerPort: 8083
        env:
        - name: PORT
          value: "8083"
        - name: ENVIRONMENT
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: database-url
        - name: REDIS_URL
          value: "valkey:6379"
        - name: AUTH_SERVICE
          value: "http://auth-service:8080"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: settings-service
  namespace: powerlifting-coach
spec:
  selector:
    app: settings-service
  ports:
  - port: 8083
    targetPort: 8083
  type: ClusterIP