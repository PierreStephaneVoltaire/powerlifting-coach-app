apiVersion: apps/v1
kind: Deployment
metadata:
  name: dm-service
  namespace: app
  labels:
    app: dm-service
spec:
  replicas: 0
  selector:
    matchLabels:
      app: dm-service
  template:
    metadata:
      labels:
        app: dm-service
    spec:


      containers:
      - name: dm-service
        image: dm-service:latest
        ports:
        - containerPort: 8084
        env:
        - name: PORT
          value: "8084"
        - name: ENVIRONMENT
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: database-url
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: rabbitmq-url
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "150m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: dm-service
  namespace: app
spec:
  selector:
    app: dm-service
  ports:
  - port: 8084
    targetPort: 8084
  type: ClusterIP
