apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: powerlifting-coach
  labels:
    app: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: ghcr.io/powerlifting-coach-app/notification-service:latest
        ports:
        - containerPort: 8086
        env:
        - name: PORT
          value: "8086"
        - name: ENVIRONMENT
          value: "production"
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: rabbitmq-url
        - name: SENDGRID_API_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: sendgrid-api-key
        - name: SENDGRID_FROM_EMAIL
          value: "noreply@powerliftingcoach.app"
        - name: USER_SERVICE
          value: "http://user-service:8081"
        - name: COACH_SERVICE
          value: "http://coach-service:8085"
        - name: PROGRAM_SERVICE
          value: "http://program-service:8084"
        - name: VIDEO_SERVICE
          value: "http://video-service:8082"
        - name: APP_URL
          value: "https://app.powerliftingcoach.app"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8086
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8086
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: powerlifting-coach
spec:
  selector:
    app: notification-service
  ports:
  - port: 8086
    targetPort: 8086
  type: ClusterIP